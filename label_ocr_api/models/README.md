# Models directory

This directory is for storing trained ML models.

Place the trained label classifier model here:
- `label_classifier.pkl` - sklearn model for classifying OCR tokens

See `../scripts/train_label_classifier.py` for training instructions.
